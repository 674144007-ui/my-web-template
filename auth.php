<?php
/**
 * ===================================================================================
 * [SYSTEM KERNEL] FILE: auth.php
 * ===================================================================================
 * หน้าที่: จัดการระบบ Session, ตรวจสอบสิทธิ์เข้าใช้งาน (RBAC), และรักษาความปลอดภัยระดับสูง
 * การแก้ไข: ขยาย Logic เพื่อรองรับระบบจำลองสิทธิ์ และป้องกันการสับสนของบทบาทผู้ใช้งาน
 * ===================================================================================
 */

// --- ตั้งค่าระบบป้องกัน Header และ Output Buffer ---
if (ob_get_level() == 0) {
    ob_start();
}

// --- ตั้งค่า session security (รักษาโครงสร้างเดิมของคุณไว้ทั้งหมด) ---
if (session_status() === PHP_SESSION_NONE) {

    /**
     * [SECURITY CONFIG] 
     * ป้องกัน Session Hijacking โดยการบังคับใช้เฉพาะ Cookies และห้าม JavaScript เข้าถึง
     */
    ini_set('session.cookie_httponly', 1); 
    ini_set('session.use_only_cookies', 1);

    // ตรวจสอบและตั้งค่า Secure Cookie หากใช้งานผ่านโปรโตคอล HTTPS
    if (!empty($_SERVER['HTTPS']) && $_SERVER['HTTPS'] !== 'off') {
        ini_set('session.cookie_secure', 1);
    }

    // กำหนดอายุการใช้งานของ Session Cookie (0 = จนกว่าจะปิด Browser)
    session_set_cookie_params(0, '/', '', false, true);

    // เริ่มต้นกระบวนการ Session
    session_start();

    /**
     * [SECURITY CONFIG] 
     * ป้องกัน Session Fixation: หากเป็นการเข้าถึงครั้งแรก ให้ทำการสุ่ม ID ใหม่ทันที
     */
    if (!isset($_SESSION['initiated'])) {
        session_regenerate_id(true);
        $_SESSION['initiated'] = true;
    }
}

/**
 * ===================================================================================
 * [FUNCTION] - isLoggedIn()
 * ตรวจสอบว่ามีข้อมูล user_id ในหน่วยความจำ Session หรือไม่
 * ===================================================================================
 */
if (!function_exists('isLoggedIn')) {
    function isLoggedIn() {
        // ตรวจสอบความถูกต้องของ user_id และสถานะความปลอดภัย
        return isset($_SESSION['user_id']) && intval($_SESSION['user_id']) > 0;
    }
}

/**
 * ===================================================================================
 * [FUNCTION] - currentUser()
 * ดึงข้อมูลสดจากหน่วยความจำ Session มาสร้างเป็น User Object (Array)
 * ===================================================================================
 */
if (!function_exists('currentUser')) {
    function currentUser() {
        if (!isLoggedIn()) {
            return null;
        }

        /**
         * 🔥 DEVELOPER AUTO-FIX:
         * บังคับล้างสิทธิ์ admin/dev ที่อาจสะกดผิดพลาด ให้เป็น 'developer' ทั้งระบบ
         */
        $role_check = strtolower(trim($_SESSION['role'] ?? 'guest'));
        if ($role_check === 'admin' || $role_check === 'dev') {
            $_SESSION['role'] = 'developer';
        }

        // คืนค่าข้อมูลสมาชิกทั้งหมด (รักษาคีย์เดิมของคุณไว้ครบถ้วน)
        return [
            'id'                 => $_SESSION['user_id'],
            'username'           => $_SESSION['username'],
            'display_name'       => $_SESSION['display_name'],
            'role'               => $_SESSION['role'],
            'class_level'        => $_SESSION['class_level'] ?? null,
            'subject_group'      => $_SESSION['subject_group'] ?? null,
            'teacher_department' => $_SESSION['teacher_department'] ?? null,
            'login_timestamp'    => $_SESSION['initiated_at'] ?? time()
        ];
    }
}

/**
 * ===================================================================================
 * [FUNCTION] - requireLogin()
 * บังคับให้หน้าเว็บหยุดการทำงานทันทีหากยังไม่ได้ผ่านการตรวจสอบตัวตน
 * ===================================================================================
 */
if (!function_exists('requireLogin')) {
    function requireLogin() {
        if (!isLoggedIn()) {
            // ล้างบัฟเฟอร์ก่อนการ Redirect เพื่อป้องกัน Error
            if (ob_get_length()) ob_clean();
            header("Location: index.php?msg=session_required");
            exit;
        }
    }
}

/**
 * ===================================================================================
 * [FUNCTION] - requireRole($roles)
 * ระบบรักษาความปลอดภัยชั้นสูงสุดที่คัดกรองบทบาทผู้ใช้งาน (Gatekeeper)
 * รองรับทั้งรูปแบบ String เดี่ยว และแบบ Array
 * ===================================================================================
 */
if (!function_exists('requireRole')) {
    function requireRole($roles) {
        // ขั้นตอนที่ 1: ตรวจสอบสถานะ Login พื้นฐาน
        requireLogin();

        // ดึงบทบาทจาก Session และล้างค่าให้เป็นมาตรฐานเดียวกัน
        $userRole = strtolower(trim($_SESSION['role']));

        // ขั้นตอนที่ 2: จัดรูปแบบพารามิเตอร์ให้เป็น Array
        if (!is_array($roles)) {
            $roles = [$roles];
        }
        
        // แปลงรายการบทบาทที่อนุญาตให้เป็นตัวเล็กทั้งหมดเพื่อการเปรียบเทียบที่แม่นยำ
        $roles = array_map('strtolower', $roles);

        /**
         * 🟢 DEVELOPER SPECIAL PASS:
         * หากผู้ใช้เป็น Developer จะได้รับสิทธิ์เข้าถึงทุกพื้นที่โดยไม่มีข้อยกเว้น
         */
        if ($userRole === 'developer') {
            return true;
        }

        // ขั้นตอนที่ 3: ตรวจสอบว่าสิทธิ์ปัจจุบันอยู่ในรายการที่ได้รับอนุญาตหรือไม่
        if (!in_array($userRole, $roles)) {
            // หากไม่มีสิทธิ์ ให้ตอบกลับด้วย HTTP Status 403 Forbidden
            http_response_code(403);

            // แสดงหน้าจอแจ้งเตือนความปลอดภัย (UI สำหรับ Access Denied)
            ?>
            <!DOCTYPE html>
            <html lang="th">
            <head>
                <meta charset="UTF-8">
                <title>403 Forbidden - ไม่อนุญาตให้เข้าถึง</title>
                <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;600&display=swap" rel="stylesheet">
                <style>
                    body { background-color: #020617; color: white; font-family: 'Prompt', sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
                    .alert-card { background: rgba(30, 41, 59, 0.7); padding: 50px; border-radius: 30px; text-align: center; border: 1px solid rgba(255, 255, 255, 0.1); box-shadow: 0 25px 50px rgba(0,0,0,0.5); backdrop-filter: blur(10px); max-width: 500px; width: 90%; }
                    .alert-card h2 { color: #ef4444; font-size: 32px; margin-bottom: 20px; }
                    .alert-card p { color: #94a3b8; font-size: 18px; margin-bottom: 10px; }
                    .role-tag { display: inline-block; padding: 5px 15px; background: #ef4444; border-radius: 50px; color: white; font-weight: bold; }
                    .btn-back { display: inline-block; margin-top: 30px; padding: 12px 35px; background: #3b82f6; color: white; text-decoration: none; border-radius: 12px; font-weight: bold; }
                </style>
            </head>
            <body>
                <div class="alert-card">
                    <div style="font-size: 60px;">🚫</div>
                    <h2>403 - ไม่อนุญาตให้เข้าถึง</h2>
                    <p>สิทธิ์การใช้งานของคุณไม่เพียงพอที่จะเข้าถึงหน้านี้</p>
                    <p>บทบาทปัจจุบัน: <span class="role-tag"><?php echo strtoupper($userRole); ?></span></p>
                    <p>สิทธิ์ที่ต้องการ: <b><?php echo implode(", ", array_map('strtoupper', $roles)); ?></b></p>
                    <a href="index.php" class="btn-back">กลับสู่หน้าหลัก</a>
                </div>
            </body>
            </html>
            <?php
            exit;
        }
    }
}
?>